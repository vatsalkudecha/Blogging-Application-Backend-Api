# db configuration
spring.datasource.url=jdbc:mysql://localhost:3306/blog_app_apis
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# aa version deprecated che... to kadach agad issue avse
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# agar koi entity hse to table direct create thai jse (create, update, create-drop, validate) 
spring.jpa.hibernate.ddl-auto=update	

spring.jpa.show-sql=true

# File related all configurations
spring.servlet.multipart.max-file-size = 10MB
spring.servlet.multipart.max-request-size = 10MB

project.image = images/

logging.level.org.springframework.security = DEBUG

spring.security.user.name = vatsal
spring.security.user.password = vatsal
spring.security.user.roles = ADMIN